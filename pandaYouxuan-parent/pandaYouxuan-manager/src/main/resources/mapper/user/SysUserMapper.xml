<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.pandaYouxuan.mapper.SysUserMapper">

    <!-- 定义可重用的SQL片段 -->
    <sql id="columns">
        id,
        username,
        password,
        name,
        phone,
        avatar,
        description,
        status,
        create_time,
        update_time,
        is_deleted
    </sql>

    <!-- 使用抽取的列进行select查询 -->
    <select id="selectByUserName" resultType="org.example.entity.system.SysUser">
        SELECT <include refid="columns" />
        FROM sys_user
        WHERE username = #{username}
        AND is_deleted = 0
    </select>

</mapper>